package com.neurogarden.app.audio

import android.content.Context

class TherapyAudioPlayer(private val context: Context) {
    fun play(soundName: String) {
        // TODO: Map soundName to bundled raw resources when production audio assets are added.
    }

    fun stop() = Unit
}
